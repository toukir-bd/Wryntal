export const metadata = {
  title: "Wryntal - New Listing",
  description: "Wryntal - New Listing",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
