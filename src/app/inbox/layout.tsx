import '../tailwind.scss'

export const metadata = {
  title: "Wryntal - Inbox",
  description: "Wryntal - Inbox",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
