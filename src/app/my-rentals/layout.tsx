import '../tailwind.scss'

export const metadata = {
    title: "Wryntal - My Rentals",
    description: "Wryntal - My Rentals",
};
  
export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
