import '../tailwind.scss'

export const metadata = {
    title: "Wryntal - Blogs",
    description: "Wryntal - Blogs",
};
  
export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
