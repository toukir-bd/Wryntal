import '../tailwind.scss'

export const metadata = {
    title: "Wryntal - Checkout",
    description: "Wryntal - Checkout",
};
  
export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
