export const metadata = {
  title: "Wryntal - Marketplace",
  description: "Wryntal - Marketplace",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
