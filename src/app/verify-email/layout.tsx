import "../tailwind.scss";

export const metadata = {
  title: "Wryntal - Verify Your Email",
  description: "Wryntal - Verify Your Email",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
