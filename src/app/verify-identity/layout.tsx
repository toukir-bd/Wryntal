export const metadata = {
  title: "Wryntal - Verify Your Identity",
  description: "Wryntal - Verify Your Identity",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
