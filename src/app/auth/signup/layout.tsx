import "../../tailwind.scss";

export const metadata = {
  title: "Wryntal - Sign Up",
  description: "Sign Up for Wryntal",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
