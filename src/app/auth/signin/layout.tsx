import '../../tailwind.scss'

export const metadata = {
  title: "Wryntal - Sign In",
  description: "Sign in to Wryntal",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
