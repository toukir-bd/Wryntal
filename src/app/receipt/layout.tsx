import '../tailwind.scss'

export const metadata = {
    title: "Wryntal - Confirmation Receipt",
    description: "Wryntal - Confirmation Receipt",
};
  
export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
